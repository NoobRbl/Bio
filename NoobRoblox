<!DOCTYPE html>
<html lang="vi">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Noob Team Bio • Premium</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;800&family=Playfair+Display:wght@600&display=swap" rel="stylesheet">
<style>
:root{
  --bg1: #160026;
  --bg2: #090014;
  --accent: #a97bff;
  --accent-2: #cc8bff;
  --card-blur: 14px;
  --radius: 18px;
}
*{box-sizing:border-box;margin:0;padding:0;}
html,body{height:100%;overflow:hidden;font-family:'Inter',sans-serif;}
body{opacity:0;transition:opacity 0.8s ease; display:flex;align-items:center;justify-content:center;padding:30px;color:#f0eaff;position:relative;}
body.fade-in{opacity:1;}
canvas{position:fixed;top:0;left:0;width:100%;height:100%;z-index:0;}

/* ==== LAYOUT ==== */
.container{
  position:relative;
  z-index:3;
  width:100%;
  max-width:920px;
  display:grid;
  grid-template-columns:280px 1fr;
  gap:28px;
}

.card{
  background:rgba(255,255,255,0.05);
  border-radius:var(--radius);
  padding:22px;
  backdrop-filter:blur(var(--card-blur));
  border:1px solid rgba(255,255,255,0.07);
  box-shadow:0 10px 35px rgba(0,0,0,0.55);
  transition:transform .5s cubic-bezier(.25,.46,.45,.94), box-shadow .4s ease;
}
.card:hover{
  transform:translateY(-6px);
  box-shadow:0 14px 50px rgba(120,40,255,0.35);
}

.avatar{
  width:220px;height:220px;border-radius:18px;overflow:hidden;margin:0 auto;
  border:1px solid rgba(255,255,255,0.08);
  box-shadow:0 8px 35px rgba(160,80,255,0.25);
  transition:transform .45s ease;
}
.avatar:hover{ transform:scale(1.06) rotate(1.5deg); }
.avatar img{ width:100%;height:100%;object-fit:cover }

.name{text-align:center;margin-top:12px}
.name h1{font-family:'Playfair Display',serif;font-size:24px;margin:0}
.name p{opacity:.85;margin-top:6px}

.meta{padding:26px}
.tags{display:flex;gap:8px;margin:16px 0;flex-wrap:wrap}
.tag{
  padding:6px 10px;
  background:rgba(255,255,255,0.05);
  border-radius:12px;
  border:1px solid rgba(255,255,255,0.1);
  font-size:13px;
  backdrop-filter:blur(4px)
}

.buttons{display:flex;gap:12px;flex-wrap:wrap;margin-top:18px}
.btn{
  padding:12px 18px;
  font-weight:600;
  border-radius:12px;
  cursor:pointer;
  transition:transform .25s ease, box-shadow .25s ease;
  border:0;
}
.btn:hover{transform:translateY(-5px);box-shadow:0 10px 30px rgba(150,80,255,0.25)}
.primary{background:linear-gradient(90deg,var(--accent),var(--accent-2));color:#14001f;}
.ghost{background:rgba(255,255,255,0.05);color:#f0eaff;border:1px solid rgba(255,255,255,0.1);}

/* ==== SOCIAL LINKS ==== */
.footer-links a.social, .footer-links div.social{
  padding:6px 10px;
  border-radius:8px;
  background:rgba(255,255,255,0.05);
  border:1px solid rgba(255,255,255,0.1);
  text-decoration:none;
  color:#f0eaff;
  font-size:14px;
  transition:transform .25s ease, box-shadow .25s ease, background 0.3s ease;
}
.footer-links a.social:hover, .footer-links div.social:hover{
  transform:translateY(-3px);
  box-shadow:0 8px 25px rgba(160,80,255,0.3);
  background:rgba(255,255,255,0.12);
}

/* ==== Nút nhạc ==== */
#music-btn{
  position:fixed;
  top:20px;
  right:20px;
  z-index:5;
  padding:10px 14px;
  font-size:16px;
  border:none;
  border-radius:12px;
  cursor:pointer;
  background:rgba(255,255,255,0.08);
  color:#f0eaff;
  backdrop-filter:blur(6px);
  transition:transform .25s ease,box-shadow .25s ease,background .3s ease;
}
#music-btn:hover{
  transform:translateY(-3px);
  box-shadow:0 8px 25px rgba(160,80,255,0.3);
  background:rgba(255,255,255,0.15);
}

/* responsive */
@media (max-width:820px){
  .container{grid-template-columns:1fr}
  .avatar{width:160px;height:160px}
}
</style>
</head>
<body>

<!-- Nhạc nền -->
<audio id="bg-music" src="https://noobrbl.github.io/Sound/snaptik.org_audio_791193c8ee37aca956adae02f6a7dc3e.mp3" loop></audio>

<!-- Nút nhạc -->
<button id="music-btn">🎵</button>

<canvas id="galaxy"></canvas>

<div class="container">

  <!-- LEFT CARD -->
  <div class="card">
    <div class="avatar">
      <img src="https://noobrbl.github.io/Img/7337076636201124871_avatar.png.jpg">
    </div>

    <div class="name">
      <h1>NOOB ROBLOX</h1>
      <p>Chuyên share, tạo script bug, cheat game Roblox</p>
    </div>

    <div class="accent-line"></div>

    <div style="text-align:center;margin-top:6px;opacity:.9">
      <strong>Role:</strong> Owner Noob • Developer
    </div>
  </div>

  <!-- RIGHT SIDE -->
  <div class="card meta">
    <h2>Xin chào — Tôi là NoobRoblox</h2>
    <p>Tất cả script của Noob Team được chia sẻ miễn phí, uy tín 100%, không scam — an toàn cho mọi người dùng.</p>

    <div class="tags">
      <div class="tag">Script</div>
      <div class="tag">OP</div>
      <div class="tag">Cheat</div>
    </div>

    <div class="buttons">
      <button class="btn primary" id="btn1">Noob Roblox</button>
      <button class="btn primary" id="btn2">MozilOnTop</button>
      <button class="btn primary" id="btn3">Thai Script</button>
      <button class="btn primary" id="btn4">Meow Script</button>
      <button class="btn primary" id="btn5">CrashXMatriz</button>

      <!-- ⭐ NÚT MỚI THÊM -->
      <button class="btn primary" id="btn6">AdPanel Hub</button>
    </div>

    <div class="footer-links" style="display:flex;gap:10px;margin-top:18px">
      <a class="social" href="https://www.tiktok.com/@scriptforyou01" target="_blank" rel="noopener noreferrer">⚡ TikTok</a>
      <div class="social">🌐 Website</div>
      <a class="social" href="https://www.tiktok.com/@scriptforyou01" target="_blank" rel="noopener noreferrer">✉️ Liên hệ</a>
    </div>
  </div>
</div>

<script>
// Fade-in
window.addEventListener("load", ()=>{document.body.classList.add("fade-in");});

// Nút + fade-out
const links={
  btn1:"https://pastefy.app/AeXwB0JS",
  btn2:"https://pastefy.app/BPHv8AbQ",
  btn3:"https://pastefy.app/o2zUBSe2",
  btn4:"https://pastefy.app/gpbqqigV",
  btn5:"https://pastefy.app/RTpitfVC", 
  btn6:"https://pastefy.app/uYa2TehD" // ⭐ Nút AdPanel Hub
};

Object.keys(links).forEach(id=>{
  const el=document.getElementById(id);
  el.addEventListener("click",()=>{
    el.animate([{transform:'scale(1)'},{transform:'scale(0.92)'},{transform:'scale(1)'}],{duration:320});
    setTimeout(()=>{
      document.body.style.opacity=0;
      setTimeout(()=>window.location.href=links[id],500);
    },150);
  });
});

// ==== Galaxy canvas nâng cấp: depth + parallax + nebula glow ====
const canvas=document.getElementById("galaxy");
const ctx=canvas.getContext("2d");
let width=canvas.width=window.innerWidth;
let height=canvas.height=window.innerHeight;

class Star{
  constructor(depth){
    this.depth=depth;
    this.x=Math.random()*width;
    this.y=Math.random()*height;
    this.size=(Math.random()*1.2+0.5)/this.depth;
    this.speed=(Math.random()*0.3+0.05)/this.depth;
    this.alpha=Math.random()*0.5+0.3;
  }
  update(mouse){
    this.x-=this.speed+mouse.x*(0.001/this.depth);
    this.y-=this.speed+mouse.y*(0.001/this.depth);
    if(this.y<0) this.y=height;
    if(this.x<0) this.x=width;
  }
  draw(){
    ctx.fillStyle=`rgba(255,255,255,${this.alpha})`;
    ctx.beginPath();
    ctx.arc(this.x,this.y,this.size,0,Math.PI*2);
    ctx.fill();
  }
}

let stars=[];
for(let d=1;d<=3;d++){
  for(let i=0;i<100;i++){
    stars.push(new Star(d));
  }
}

function drawNebula(){
  let gradient=ctx.createRadialGradient(width/2,height/2,width*0.1,width/2,height/2,width*0.9);
  gradient.addColorStop(0,'rgba(120,0,255,0.15)');
  gradient.addColorStop(0.5,'rgba(80,0,180,0.1)');
  gradient.addColorStop(1,'rgba(0,0,0,0)');
  ctx.fillStyle=gradient;
  ctx.fillRect(0,0,width,height);
}

let mouse={x:0,y:0};
window.addEventListener("mousemove",e=>{mouse.x=e.clientX-width/2;mouse.y=e.clientY-height/2;});

function animate(){
  ctx.fillStyle="#160026";
  ctx.fillRect(0,0,width,height);
  drawNebula();
  for(let s of stars){s.update(mouse);s.draw();}
  requestAnimationFrame(animate);
}
animate();

window.addEventListener("resize",()=>{
  width=canvas.width=window.innerWidth;
  height=canvas.height=window.innerHeight;
});

// ==== Nút nhạc + auto play sau click đầu tiên ====
const musicBtn=document.getElementById("music-btn");
const bgMusic=document.getElementById("bg-music");

// Bắt nhạc sau click đầu tiên
function startMusic(){
  bgMusic.play().catch(()=>{}); 
  window.removeEventListener("click", startMusic);
}
window.addEventListener("click", startMusic);

// Bật/tắt nhạc
musicBtn.addEventListener("click",()=>{
  if(bgMusic.paused){
    bgMusic.play();
    musicBtn.textContent="🎵";
  }else{
    bgMusic.pause();
    musicBtn.textContent="🔇";
  }
});
</script>

</body>
</html>
